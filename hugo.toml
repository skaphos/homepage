baseURL = 'https://skaphos.io/'
languageCode = 'en-us'
title = 'Skaphos'
disableKinds = ['taxonomy', 'term', 'RSS']

[params]
  tagline = 'Navigate your infrastructure.'
  summary = 'Open control-plane tooling for platform teams that operate Kubernetes — not a dashboard over it.'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
